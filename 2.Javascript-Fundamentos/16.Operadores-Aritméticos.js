const [a, b, c, d] = [3, 4, 5, 6]

// esses são operadores binários

const soma = a + b + c + d
const subtracao = d - a
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

// -d, por exemplo, é um operador unário

console.log(soma, subtracao, multiplicacao, divisao, modulo)